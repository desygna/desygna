import { system } from "styled-system";

export const $shadow = system({
  textShadow: {
    property: "color",
    scale: "shadows"
  },
  boxShadow: {
    property: "color",
    scale: "shadows"
  }
});
